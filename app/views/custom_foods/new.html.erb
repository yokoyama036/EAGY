<%= form_with(model: @custom_food, local: true) do |form| %>
  <div class="form-group">
    <%= form.label :食材名 %>
    <%= form.text_field :name, class: 'form-control', required: true, id: 'custom_food_name', placeholder: '追加したい食材名を入力してください' %>
  </div>
  <%= link_to 'AIで栄養素を取得', '#', class: 'btn btn-secondary', id: 'fetch-nutrient-data' %>
  <div id="loader-bg" class="is-hide">
    <div id="loader" class="is-hide">
        <p>
          <div class="ball-pulse"><div></div><div></div><div></div></div>
        </p>
    </div>
</div>
<div class="form-group">
    <%= form.label :calorie, 'カロリー' %>
    <%= form.number_field :calorie, step: "any", class: 'form-control', min: 0, required: true, id: 'custom_food_calorie', placeholder: 'カロリーを入力してください' %>
  </div>

  <div class="form-group">
    <%= form.label :protein, 'タンパク質' %>
    <%= form.number_field :protein, step: "any", class: 'form-control', min: 0, required: true, id: 'custom_food_protein', placeholder: 'タンパク質量を入力してください' %>
  </div>

  <div class="form-group">
    <%= form.label :carbo, '炭水化物' %>
    <%= form.number_field :carbo, step: "any", class: 'form-control', min: 0, required: true, id: 'custom_food_carbo', placeholder: '炭水化物量を入力してください' %>
  </div>

  <div class="form-group">
    <%= form.label :fat, '脂質' %>
    <%= form.number_field :fat, step: "any", class: 'form-control', min: 0, required: true, id: 'custom_food_fat', placeholder: '脂質量を入力してください' %>
  </div>

  <div class="form-group">
    <%= form.label :salt, '塩分' %>
    <%= form.number_field :salt, step: "any", class: 'form-control', min: 0, required: true, id: 'custom_food_salt', placeholder: '塩分量を入力してください' %>
  </div>

  <div class="form-group">
    <%= form.label :unit, '単位数量' %>
    <%= form.text_field :unit, step: "any", class: 'form-control', required: true, id: 'custom_food_unit', placeholder: '単位数量を入力 例:100g、大さじ1等' %>
  </div>

  <div class="form-group">
    <%= form.submit '登録', step: "any", class: 'btn btn-primary' %>
  </div>
<% end %>
<h4>栄養素情報自動入力に関する注意事項</h4>
<h5>栄養素情報の正確性：自動入力された栄養素情報は、ChatGPT APIによって提供されています。</h5>
<h5>APIは一般的に高い精度を持っていますが、必ずしも正確な情報が提供されるわけではありません。</h5>
<h5>自動入力された情報が正確かどうかを確認するようお願いいたします。</h5>
<h5>入力エラー：入力された食材名によってはエラーがかえり、栄養素情報の自動入力が正常に機能しない可能性があります。</h5>
<h5>エラーが発生した場合は、手動で情報を入力してください。</h5>


